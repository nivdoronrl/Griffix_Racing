# Dependencies
node_modules/

# Environment secrets — NEVER commit
.env

# Credentials — never commit any of these
server/credentials.json
server/client_secret*.json
server/*.json

# macOS
.DS_Store

# Temporary screenshots
temporary screenshots/

# Logs
*.log
npm-debug.log*

# Uploaded user files — new uploads added via admin panel
# (existing images are committed so they work on Render)
uploads/.gitkeep

# Live order data — contains customer PII, never commit
server/data/orders.json

# Reference/scratch screenshots in root
us.skda.com.au_.png
