# ── Server ────────────────────────────────────────────
PORT=3000

# ── Google Sheets ──────────────────────────────────────
# The ID from the URL: docs.google.com/spreadsheets/d/<SHEET_ID>/edit
GOOGLE_SHEET_ID=
# Path to the service account JSON credentials file
GOOGLE_CREDENTIALS_PATH=./server/credentials.json

# ── Shippo Shipping ────────────────────────────────────
SHIPPO_API_KEY=
SHIPPO_FROM_NAME=Griffix Racing
SHIPPO_FROM_STREET=
SHIPPO_FROM_CITY=
SHIPPO_FROM_STATE=
SHIPPO_FROM_ZIP=
SHIPPO_FROM_COUNTRY=AU
SHIPPO_FROM_PHONE=

# ── Email (Gmail SMTP) ─────────────────────────────────
# Enable "Less secure app access" or use an App Password (recommended with 2FA)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
OWNER_EMAIL=

# ── Payments (links only — no gateway) ────────────────
# e.g. https://www.paypal.me/YourName
PAYPAL_ME_URL=

# ── Admin ──────────────────────────────────────────────
# The password you will enter on the admin login page
ADMIN_PASSWORD=
# A long random secret used to sign admin tokens (generate with: openssl rand -hex 32)
ADMIN_SECRET=
